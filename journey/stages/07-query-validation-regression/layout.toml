failure_files = ["tests/test_query.py"]

[[blocks]]
id = "mechanism"
title_en = "Query validation before planning mechanism"
title_zh = "规划前的查询校验机制"
summary_en = "Collection validates once at the public boundary; matcher can then evaluate candidates under the same recursively checked operator contract."
summary_zh = "Collection 在公共边界统一校验；Matcher 随后按同一套递归检查过的算子契约评估候选。"
files = ["src/minimongodb/query/matcher.py"]

[[blocks]]
id = "supporting"
title_en = "Package, fixture, and project support"
title_zh = "包、Fixture 与工程支撑"
summary_en = "Keep exports, test corpora, dependencies, and the runtime environment reproducible."
summary_zh = "保持包导出、测试语料、依赖与运行环境可复现。"
files = ["src/minimongodb/query/__init__.py"]
supporting = true
